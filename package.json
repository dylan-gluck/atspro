{
  "name": "atspro",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "check-types": "turbo check-types",
    "test": "turbo test",
    "format": "turbo format",
    "docker:dev": "./docker/scripts/dev.sh",
    "docker:prod": "./docker/scripts/prod.sh",
    "docker:stop": "docker-compose down",
    "docker:clean": "docker-compose down -v --remove-orphans"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "dependencies": {
    "playwright": "^1.54.2"
  }
}
